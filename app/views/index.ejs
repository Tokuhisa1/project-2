<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">

  <!-- Sets the size of the window equal to the size of the screen -->
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Tells the browser how to render the page in Microsoft Edge -->
  <meta http-equiv="X-UA-Compatible" content="ie=edge">

  <title>Como Say What!?</title>
  <link rel="stylesheet" type="text/css" href="../public/css/reset.css">
  <link rel="stylesheet" type="text/css" href="../public/css/style.css">
</head>
<body>
  <div>
    <h1><%= message %></h1>
    <h3><%= subTitle %></h3>
  </div>

  <div>
    <h4>Como Say What!? is a personal dictionary for language learners.</h4>
  </div>

  <p>There are words in <% languages.forEach((language, index) => { %>
    <% if (index != languages.length - 1) { %>
      <%= language %>,
    <% } else { %>
      and <%- language -%>.
    <% } %>
  <% }) -%></p>
</body>
</html>
